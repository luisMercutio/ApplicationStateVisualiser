PENDING BUSINESS RULE — apply to the ApplicationStateManager DB when free
==========================================================================

WHY THIS FILE EXISTS
--------------------
A code change landed but its matching Business Rule could NOT be written to the
database yet: the API server (http://localhost:3001) was down, and another
worktree was doing DB work at the time, so writing was deferred to avoid a clash.

This file records the BR to create (BR-068) plus exact directions to apply it.
Delete this file once the BR is in the DB.

CONTEXT OF THE CODE CHANGE
--------------------------
Branch:  claude-sessions-terminal-nav   (off main)
Commit:  8a663be  "Claude Sessions: Attach opens the Terminal page; add Kill for running sessions"

What changed:
- New root WorkspaceService owns the active page + a one-shot "open Terminal on
  this session" channel. app.component binds its page to it.
- Claude Sessions "Attach" no longer embeds a terminal inline; it navigates to the
  Terminal page, which consumes the requested session from WorkspaceService and
  connects.
- Running sessions get a Kill button (+ row-menu item) that ends the tmux session
  via a NEW endpoint POST /api/claude/sessions/:session/kill (tmux kill-session).
  The worktree and archived transcript are kept, so the session stays reopenable.

DEPENDENCY / DUPLICATE CHECK BEFORE APPLYING
--------------------------------------------
- BR-067 (dead sessions + Reopen + View conversation) was ALREADY written to the
  DB in an earlier session. Do NOT recreate it — just verify it exists.
- Confirm the next free BR number is still 068 (another worktree may have added
  BRs in the meantime). If 068 is taken, bump the name/seq to the next free number
  but keep the content below.

HOW TO APPLY (per CLAUDE.md "Recording a BR for a code change")
---------------------------------------------------------------
1. Ensure the server is up:   curl http://localhost:3001/api/ping
2. Get the connection id (do NOT hardcode; it can change):
     curl http://localhost:3001/api/db/connections
   Pick the entry named "ApplicationStateManager". Call its id <CID>.
   (At last check it was 43907b34-e382-4160-99a8-0bda6eb95ece — verify.)
3. Confirm the epic id for EPIC-012 "Submit Business Rules with Claude":
     curl http://localhost:3001/api/db/connections/<CID>/epics
   (At last check: 6de93b62-8097-45dd-890e-02647282d226 — verify.)
4. Confirm BR-067 exists and find the max BR number:
     curl http://localhost:3001/api/db/connections/<CID>/business-rules
5. POST the BR below (substitute <CID> and, if needed, the epicId):

curl -X POST http://localhost:3001/api/db/connections/<CID>/business-rules \
  -H 'Content-Type: application/json' \
  -d '{
    "name": "BR-068",
    "epicId": "6de93b62-8097-45dd-890e-02647282d226",
    "seq": "68",
    "rule": "On the Claude Sessions page, Attach opens the Terminal page focused on the selected tmux session (rather than an inline terminal), and a running session can be killed from its row.",
    "rationale": "A root WorkspaceService owns the active page and a one-shot terminal-session handoff, so Attach navigates to the Terminal view which consumes the requested session and connects. Kill calls POST /api/claude/sessions/:session/kill (tmux kill-session); the worktree and archived transcript are preserved so the session remains reopenable.",
    "category": "ui",
    "features": ["claude-sessions", "terminal"],
    "dependsOn": ["BR-066", "BR-067"],
    "touches": {
      "frontend": [
        "src/app/services/workspace.service.ts",
        "src/app/app.component.ts",
        "src/app/components/views/terminal/terminal.component.ts",
        "src/app/components/views/claude-sessions/claude-sessions.component.ts",
        "src/app/services/file.service.ts"
      ],
      "backend": ["server.js"]
    }
  }'

6. Verify it was created:
     curl http://localhost:3001/api/db/connections/<CID>/business-rules | grep BR-068
7. Delete this file (PENDING-DB-BR-068.txt).
