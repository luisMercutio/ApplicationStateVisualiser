PENDING BUSINESS RULE — apply to the ApplicationStateManager DB when free
==========================================================================

WHY THIS FILE EXISTS
--------------------
A code change landed but its Business Rule could not be written to the database
yet (the API server was down). This file records BR-069 plus directions to apply
it. Delete this file once the BR is in the DB.

⚠️ API MAY HAVE CHANGED (oneDB refactor)
----------------------------------------
The "Collapse multi-DB architecture into a single store with an applications
entity" work (branch worktree-oneDB, commit 618e6b9) changes the BR API from the
OLD per-connection shape to a NEW per-application shape. The PreToolUse hook now
advertises the NEW shape. Before applying, detect which is live:
  - NEW:  GET /api/applications  → pick "ApplicationStateManager" → <APPID>;
          then /api/applications/<APPID>/epics and /business-rules;
          POST /api/applications/<APPID>/business-rules
  - OLD:  GET /api/db/connections → pick "ApplicationStateManager" → <CID>;
          then /api/db/connections/<CID>/epics and /business-rules;
          POST /api/db/connections/<CID>/business-rules
Use whichever endpoint the running server actually exposes.
NOTE: PENDING-DB-BR-068.txt was written against the OLD shape — apply the same
detection to it.

CONTEXT OF THE CODE CHANGE
--------------------------
Branch:  claude-sessions-archive-delete   (off main)
Commit:  a84ccd4  "Claude Sessions: archive or delete a dead session"

What changed:
- Dead-session rows get two menu actions. Archive removes the session's git
  worktree(s)+branch(es) (ASV-worktrees/<slug>-YYYYMMDD) and files its saved
  conversation under .claude/conversations/archived/ (kept). Delete removes the
  worktree(s)+branch(es) AND the conversation.
- Server: POST /api/claude/sessions/:session/archive and
  DELETE /api/claude/sessions/:session. Both 409 if the session is running.
  removeSessionGitFootprint only touches ASV-worktrees paths (never main/test).

DUPLICATE / DEPENDENCY CHECK
----------------------------
- BR-067 (dead sessions + Reopen + View conversation) is already in the DB.
- BR-068 (Attach→Terminal nav + Kill) is PENDING in PENDING-DB-BR-068.txt — apply
  it first (or confirm it exists) so BR-069's dependsOn resolves.
- Confirm 069 is still the next free BR number before posting; bump if taken.

THE BR (substitute <APPID> or <CID> per the detected API)
---------------------------------------------------------
POST <the /business-rules endpoint that exists>  with body:
{
  "name": "BR-069",
  "epicId": "6de93b62-8097-45dd-890e-02647282d226",   // EPIC-012 (verify via /epics)
  "seq": "69",
  "rule": "A dead Claude session can be archived (its git worktree and branch are removed and its saved conversation is filed under .claude/conversations/archived/) or deleted (worktree, branch, and conversation all removed); neither is offered while the session is running.",
  "rationale": "Archive/Delete row actions call POST /api/claude/sessions/:session/archive and DELETE /api/claude/sessions/:session. The server removes only ASV-worktrees/<slug>-YYYYMMDD worktrees and claude/<slug>[-date] branches (never the main or test worktree), and either moves the transcript into conversations/archived/ (archive) or unlinks it (delete). Both return 409 for a live session so it must be killed first.",
  "category": "ui",
  "features": ["claude-sessions"],
  "dependsOn": ["BR-067", "BR-068"],
  "touches": {
    "frontend": [
      "src/app/components/views/claude-sessions/claude-sessions.component.ts",
      "src/app/services/file.service.ts"
    ],
    "backend": ["server.js"]
  }
}

Then verify it was created, and delete this file.
