Renamed to `/epic-prep` (Epic/BR methodology). Analyze cross-Epic preparation opportunities.

This command was renamed. **Use `/epic-prep` instead** — it does exactly what
`/uc-prep` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-prep.md`
and follow it with the same `$ARGUMENTS`.
