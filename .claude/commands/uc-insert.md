Renamed to `/epic-insert`. Shim that forwards to the Epic/BR insert command.

This command was renamed. **Use `/epic-insert` instead** — it does exactly what `/uc-insert` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-insert.md` and follow it with the same `$ARGUMENTS`.
