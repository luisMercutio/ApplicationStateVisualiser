Renamed to `/epic-cleanup` (Epic/BR methodology). Merge an Epic's branch, remove its worktree and branch.

This command was renamed. **Use `/epic-cleanup` instead** — it does exactly what
`/uc-cleanup` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-cleanup.md`
and follow it with the same `$ARGUMENTS`.
