Renamed to `/epic-status` (Epic/BR methodology). Show the implementation status of the Epic chain.

This command was renamed. **Use `/epic-status` instead** — it does exactly what
`/uc-status` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-status.md`
and follow it with the same `$ARGUMENTS`.
