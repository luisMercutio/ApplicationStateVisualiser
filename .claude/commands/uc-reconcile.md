Renamed to `/epic-reconcile` (Epic/BR methodology). Reconcile architecture artifacts with the implementation.

This command was renamed. **Use `/epic-reconcile` instead** — it does exactly what
`/uc-reconcile` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-reconcile.md`
and follow it with the same `$ARGUMENTS`.
