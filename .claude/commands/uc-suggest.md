Renamed to `/epic-suggest`. Shim that forwards to the Epic/BR suggestion command.

This command was renamed. **Use `/epic-suggest` instead** — it does exactly what `/uc-suggest` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-suggest.md` and follow it with the same `$ARGUMENTS`.
