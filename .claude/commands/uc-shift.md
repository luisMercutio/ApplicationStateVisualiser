Renamed to `/epic-shift`. Shim that forwards to the Epic/BR shift command.

This command was renamed. **Use `/epic-shift` instead** — it does exactly what `/uc-shift` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-shift.md` and follow it with the same `$ARGUMENTS`.
