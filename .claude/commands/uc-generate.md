Renamed to `/epic-generate`. Shim that forwards to the Epic/BR generation command.

This command was renamed. **Use `/epic-generate` instead** — it does exactly what `/uc-generate` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-generate.md` and follow it with the same `$ARGUMENTS`.
