Renamed to `/epic-enrich` (Epic/BR methodology). Scan the Epic chain and enrich a suggestion with chain awareness.

This command was renamed. **Use `/epic-enrich` instead** — it does exactly what
`/uc-enrich` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-enrich.md`
and follow it with the same `$ARGUMENTS`.
