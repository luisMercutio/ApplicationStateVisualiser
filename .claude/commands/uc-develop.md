Renamed to `/epic-develop`. Shim that forwards to the Epic/BR development command.

This command was renamed. **Use `/epic-develop` instead** — it does exactly what `/uc-develop` used to do, in the Epic/BR methodology. Read `.claude/commands/epic-develop.md` and follow it with the same `$ARGUMENTS`.
